(deffun (f x) (+ x 1))
(defgen (g)
  (print "started")
  (yield (f 1))
  (print "yielded once")
  (yield (f 2))
  (print "yielded twice")
  (yield (f 3))
  (print "yielded third times and end"))
(defvar h (g))
(print (next h))
(print (next h))
(print (next h))
(print (next h))
